<label>Programme name <input name="title" placeholder="e.g. Rural Evangelism" value="{{ old('title', $programme->title ?? '') }}" required></label>
<label>Short description <textarea name="description" placeholder="A short description for visitors" required>{{ old('description', $programme->description ?? '') }}</textarea></label>
<div class="admin-form-row"><label>Icon <input name="icon" placeholder="fa-city" value="{{ old('icon', $programme->icon ?? 'fa-hands-helping') }}" required><small>Use a Font Awesome name, e.g. <code>fa-city</code>.</small></label><label>Accent colour <select name="colour">@foreach(['yellow','green','blue','red','purple'] as $colour)<option value="{{ $colour }}" @selected(old('colour', $programme->colour ?? 'green') === $colour)>{{ ucfirst($colour) }}</option>@endforeach</select></label></div>
<div class="admin-form-row"><label>Display order <input type="number" name="display_order" min="1" value="{{ old('display_order', $programme->display_order ?? 1) }}" required></label><label>Visibility <select name="status"><option value="active" @selected(old('status', $programme->status ?? 'active') === 'active')>Live on website</option><option value="inactive" @selected(old('status', $programme->status ?? '') === 'inactive')>Hidden</option></select></label></div>
<div class="admin-form-footer">@if($cancel ?? false)<button type="button" class="admin-quiet-button" onclick="this.closest('dialog').close()">Cancel</button>@endif<button class="admin-add-button">{{ $button }}</button></div>
